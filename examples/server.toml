# silph-server configuration

# Address to listen on (query API + dashboard). Neither has authentication,
# so keep this on loopback unless a reverse proxy in front provides TLS/auth.
listen = "127.0.0.1:8080"

# Directory for the time-series database.
data_dir = "./data"

scrape_interval = "15s"
scrape_timeout = "5s"

# Data older than this is expired.
retention = "30d"

# One entry per monitored host.
[[targets]]
name = "example-host"
url = "http://127.0.0.1:9100"
token = "change-me"
